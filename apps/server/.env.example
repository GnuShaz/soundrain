DATABASE_URL=postgres://soundrain:soundrain@localhost:5432/soundrain
PORT=3001
# Общий секрет для заголовка X-Api-Key — обязателен при разворачивании
# наружу (см. main.ts), не нужен для локальной разработки.
API_KEY=
