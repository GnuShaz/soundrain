services:
  postgres:
    image: postgres:17
    environment:
      POSTGRES_USER: soundrain
      POSTGRES_PASSWORD: soundrain
      POSTGRES_DB: soundrain
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U soundrain"]
      interval: 5s
      timeout: 3s
      retries: 5

volumes:
  postgres_data:
